<noscript><iframe src="https://www.googletagmanager.com/ns.html?id=GTM-TKN8V373"
height="0" width="0" style="display:none;visibility:hidden"></iframe></noscript>
<style>
  /* Scoped styles for header search bar */
  .navbar-brand img {
    height: 50px !important;
  }

  .navbar .search-bar {
    margin-left: 0.75rem;
    margin-right: 0.75rem;
  }

  .navbar .search-input {
    border-radius: 50rem;
    padding: 0.625rem 2.75rem 0.625rem 1.25rem;
    height: 2.75rem;
    /* Larger touch target */
    line-height: 1.5;
    font-size: 1rem;
    /* Improved readability */
    background-color: #ffffff;
    transition: background-color 0.2s ease;
  }

  .navbar .search-input::placeholder {
    color: #6c757d;
    font-size: 0.95rem;
  }

  .navbar .search-input:focus {
    background-color: #ffffff;
    box-shadow: 0 0 0 0.25rem rgba(13, 110, 253, 0.25);
  }

  .navbar .search-icon {
    color: #6c757d;
    font-size: 1.125rem;
    /* Larger icon */
    pointer-events: none;
    z-index: 3;
  }

  .nav-link {
    transition: color 0.2s ease;
  }

  .nav-link.contactLink {
    --bs-nav-link-padding-x: 1rem;
    --bs-nav-link-padding-y: 0.5rem
  }

  .navbar .nav-icon {
    font-size: 1.25rem;
    /* Larger icons for contact/profile */
  }

  /* Mobile adjustments */
  @media (max-width: 575.98px) {
    .navbar .search-input {
      font-size: 0.95rem;
      height: 2.5rem;
      padding: 0.5rem 2.5rem 0.5rem 1rem;
    }

    .navbar .search-input::placeholder {
      font-size: 0.9rem;
    }

    .navbar .search-icon {
      font-size: 1rem;
    }

    .navbar .search-bar {
      margin-left: 0.5rem;
      margin-right: 0.5rem;
    }
  }

  /* Tablet adjustments */
  @media (min-width: 576px) and (max-width: 767.98px) {
    .navbar .search-input {
      font-size: 0.975rem;
      height: 2.625rem;
    }

    .navbar .search-input::placeholder {
      font-size: 0.925rem;
    }

    .navbar .search-icon {
      font-size: 1.1rem;
    }
  }

  /* Hide search bar on larger screens */
  @media (min-width: 992px) {
    .navbar-nav {
      --bs-nav-link-padding-x: 0.5rem;
      --bs-nav-link-padding-y: 1rem;
    }
  }

  /*# sourceMappingURL=style.css.map */
  /* Custom styles to match the exact UI */
  /* ── Sign In Modal ── */
  #signinModal .modal-dialog { max-width: 420px; }
  #signinModal .modal-content {
    border-radius: 20px; border: none; padding: 0;
    box-shadow: 0 8px 40px rgba(0,0,0,0.18);
  }
  #signinModal .modal-header,
  #signinModal .modal-footer { border: none; padding: 0; background: transparent; }
  #signinModal .modal-body { padding: 32px 32px 24px; }

  #signinModal .auth-close {
    position: absolute; top: 14px; right: 16px; z-index: 10;
    font-size: 1.1rem; background: none; border: none; cursor: pointer; color: #555; line-height: 1;
  }
  #signinModal .auth-close:hover { color: #111; }
  #signinModal .auth-logo { display: block; height: 56px; margin: 0 auto 16px; object-fit: contain; }
  #signinModal .auth-welcome { text-align: center; font-size: 0.95rem; color: #555; margin-bottom: 2px; }
  #signinModal .auth-brand { text-align: center; font-size: 1.2rem; font-weight: 700; color: #111; margin-bottom: 22px; }

  #signinModal .auth-divider {
    display: flex; align-items: center; gap: 10px;
    color: #6b7280; font-size: 0.82rem; margin-bottom: 20px;
  }
  #signinModal .auth-divider::before,
  #signinModal .auth-divider::after { content: ''; flex: 1; height: 1px; background: #d1d5db; }

  #signinModal .auth-phone-group {
    display: flex; align-items: center;
    border: 1.5px solid #d1d5db; border-radius: 50px;
    padding: 0 18px; height: 54px; margin-bottom: 14px;
    transition: border-color 0.2s;
  }
  #signinModal .auth-phone-group:focus-within { border-color: #EF4F5F; }
  #signinModal .auth-phone-prefix {
    font-weight: 600; font-size: 0.95rem; color: #111;
    padding-right: 12px; border-right: 1.5px solid #e5e7eb; margin-right: 12px; white-space: nowrap; flex-shrink: 0;
  }
  #signinModal .auth-phone-input {
    border: none; outline: none; flex: 1; font-size: 0.95rem; background: transparent; width: 100%;
  }

  #signinModal .auth-continue-btn {
    width: 100%; height: 52px; border-radius: 50px;
    background: #EF4F5F; color: #fff;
    font-size: 1rem; font-weight: 600; border: none;
    cursor: pointer; transition: background 0.18s; margin-bottom: 16px;
  }
  #signinModal .auth-continue-btn:hover:not(:disabled) { background: #d63d4d; }
  #signinModal .auth-continue-btn:disabled { background: #e5e7eb; color: #9ca3af; cursor: not-allowed; }

  #signinModal .auth-or-divider {
    display: flex; align-items: center; gap: 10px;
    color: #6b7280; font-size: 0.82rem; margin-bottom: 14px;
  }
  #signinModal .auth-or-divider::before,
  #signinModal .auth-or-divider::after { content: ''; flex: 1; height: 1px; background: #d1d5db; }

  #signinModal .auth-google-btn {
    width: 100%; height: 50px; border-radius: 50px;
    border: 1.5px solid #d1d5db; background: #fff;
    display: flex; align-items: center; justify-content: center;
    gap: 10px; font-size: 0.88rem; font-weight: 500; color: #111;
    cursor: pointer; margin-bottom: 20px; transition: background 0.18s; text-decoration: none;
  }
  #signinModal .auth-google-btn:hover { background: #f9fafb; color: #111; }

  #signinModal .auth-legal { text-align: center; font-size: 0.75rem; color: #6b7280; line-height: 1.6; margin: 0; }
  #signinModal .auth-legal a { color: #111; font-weight: 500; text-decoration: underline; }

  /* ── OTP Verify Modal ── */
  #verifyModal .modal-dialog { max-width: 420px; }
  #verifyModal .modal-content {
    border-radius: 20px; border: none; padding: 0;
    box-shadow: 0 8px 40px rgba(0,0,0,0.18);
  }
  #verifyModal .modal-header,
  #verifyModal .modal-footer { border: none; padding: 0; background: transparent; }
  #verifyModal .modal-body { padding: 32px 32px 28px; text-align: center; }

  #verifyModal .auth-close {
    position: absolute; top: 14px; right: 16px; z-index: 10;
    font-size: 1.1rem; background: none; border: none; cursor: pointer; color: #555; line-height: 1;
  }
  #verifyModal .auth-close:hover { color: #111; }
  #verifyModal .auth-logo-sm { display: block; height: 42px; margin: 0 auto 18px; object-fit: contain; }
  #verifyModal .auth-otp-title { font-size: 1.1rem; font-weight: 700; color: #111; margin-bottom: 6px; }
  #verifyModal .auth-otp-sub { font-size: 0.85rem; color: #6b7280; margin-bottom: 24px; }

  #verifyModal .otp-container { display: flex; justify-content: center; gap: 10px; margin-bottom: 20px; }
  #verifyModal .otp-input {
    width: 52px; height: 58px; text-align: center;
    font-size: 1.4rem; font-weight: 700;
    border: 1.5px solid #d1d5db; border-radius: 10px; outline: none;
    transition: border-color 0.2s, box-shadow 0.2s;
  }
  #verifyModal .otp-input:focus { border-color: #EF4F5F; box-shadow: 0 0 0 3px rgba(239,79,95,0.12); }

  #verifyModal .auth-verify-btn {
    width: 100%; height: 52px; border-radius: 50px;
    background: #EF4F5F; color: #fff;
    font-size: 1rem; font-weight: 600; border: none;
    cursor: pointer; transition: background 0.18s; margin-bottom: 16px;
  }
  #verifyModal .auth-verify-btn:hover:not(:disabled) { background: #d63d4d; }
  #verifyModal .auth-verify-btn:disabled { background: #e5e7eb; color: #9ca3af; cursor: not-allowed; }

  #verifyModal .resend-link { font-size: 0.85rem; color: #EF4F5F; font-weight: 500; text-decoration: none; }
  #verifyModal .resend-link:hover { text-decoration: underline; }
  #verifyModal .edit-icon { margin-left: 6px; cursor: pointer; font-size: 0.95rem; color: #EF4F5F; }

  /* ── Mobile top-bar actions (phone + login/user icon) ── */
  .navbar-phone-pill {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: #f4f6f9;
    color: #1A3C6C;
    font-size: 1rem;
    text-decoration: none;
    flex-shrink: 0;
  }
  .navbar-phone-pill:hover { background: #e8ecf2; color: #1A3C6C; }

  .navbar-login-pill {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    padding: 6px 14px;
    border-radius: 50px;
    background: #EF4F5F;
    color: #fff;
    font-size: 0.82rem;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .navbar-login-pill:hover { background: #d63d4d; color: #fff; }

  /* ── Desktop collapsed nav ─────────────────────────────── */
  /* Login pill in desktop nav */
  #topNavbar .loginBtn {
    background: #EF4F5F;
    color: #fff !important;
    border-radius: 50px !important;
    padding: 7px 20px !important;
    font-weight: 600;
  }
  #topNavbar .loginBtn:hover { background: #d63d4d; }

  /* ── Mobile collapsed nav (hamburger menu items) ──────── */
  @media (max-width: 991.98px) {
    #topNavbar {
      border-top: 1px solid #f0f0f0;
      padding: 8px 0;
    }
    #topNavbar .nav-link {
      padding: 10px 4px !important;
      font-size: 0.95rem;
      border-bottom: 1px solid #f5f5f5;
      color: #1a1a1a;
    }
    #topNavbar .nav-link:last-child { border-bottom: none; }
    #topNavbar .nav-link.active { color: #1A3C6C; font-weight: 600; }
    /* Hide phone+login from hamburger menu on mobile — they live in top bar */
    #topNavbar .contactLink,
    #topNavbar .loginBtn { display: none !important; }
  }
</style>

<!-- Meta Pixel Code -->
<script>
!function(f,b,e,v,n,t,s)
{if(f.fbq)return;n=f.fbq=function(){n.callMethod?
n.callMethod.apply(n,arguments):n.queue.push(arguments)};
if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
n.queue=[];t=b.createElement(e);t.async=!0;
t.src=v;s=b.getElementsByTagName(e)[0];
s.parentNode.insertBefore(t,s)}(window, document,'script',
'https://connect.facebook.net/en_US/fbevents.js');
fbq('init', '1118406830120831');
fbq('track', 'PageView');
</script>
<noscript><img height="1" width="1" style="display:none"
src="https://www.facebook.com/tr?id=1118406830120831&ev=PageView&noscript=1"
/></noscript>
<!-- End Meta Pixel Code -->



<!-- Bootstrap Icons already included in index.ejs -->
<nav class="navbar navbar-expand-lg bg-light sticky-top">
  <div class="container-xl gap-2">
    <button class="navbar-toggler px-2 border-0 outline-0" type="button" data-bs-toggle="collapse"
      data-bs-target="#topNavbar" aria-controls="topNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand me-auto" href="/">
      <img src="/img/logo_2x.png" alt="Deja Vu" height="50" class="d-inline-block align-text-top object-fit-contain"
        fetchpriority="high" loading="eager">
    </a>

    <!-- Phone + Auth — always visible in top bar on mobile, hidden on lg (shown in nav instead) -->
    <div class="d-flex d-lg-none align-items-center gap-2 navbar-mobile-actions">
      <a class="navbar-phone-pill" href="tel:+918511117891" aria-label="Call us">
        <i class="bi bi-telephone-fill"></i>
      </a>
      <% if(profile?.email || accessToken) { %>
        <span class="dropdown">
          <a class="nav-link userDropdown dropdown-toggle p-0" href="#" role="button"
            data-bs-toggle="dropdown" aria-expanded="false">
            <i class="bi bi-person-circle" style="font-size:1.5rem;color:#1A3C6C;"></i>
          </a>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <ul class="dropdown-menu dropdown-menu-end navbaruserdropdown">
            <li><a class="dropdown-item" href="/profile">My Profile</a></li>
            <% if(profile?.email===process.env.GMAIL_ADMIN || profile?.email===process.env.GMAIL_ADMIN_TWO) { %>
              <li><a class="dropdown-item" href="/admin/bookings" target="_blank">Bookings</a></li>
              <li>
                <a class="dropdown-item position-relative" href="/admin/booking-leads" target="_blank">
                  Booking Leads
                  <span id="pendingLeadsBadgeMobile" class="position-absolute top-50 end-0 translate-middle-y badge rounded-pill bg-danger" style="display:none;">
                    <span id="pendingLeadsCountMobile">0</span>
                  </span>
                </a>
              </li>
              <li><a class="dropdown-item" href="/admin/bookings/create" target="_blank">Create Booking</a></li>
              <li><a class="dropdown-item" href="/admin/users" target="_blank">User</a></li>
              <li><a class="dropdown-item" href="/admin/addtours" target="_blank">Add Tours</a></li>
              <li><a class="dropdown-item" href="/cities" target="_blank">Master setting</a></li>
              <li><a class="dropdown-item" href="/admin/category-trip-order" target="_blank">Homepage Categories</a></li>
            <% } %>
            <li>
              <form id="my_form_mobile" action="/logout" method="post">
                <a class="dropdown-item" href="javascript:{}" onclick="signout()">Sign Out</a>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              </form>
            </li>
          </ul>
        </span>
      <% } else { %>
        <a class="navbar-login-pill" role="button" onclick="openLogin()">
          <i class="bi bi-person"></i> Login
        </a>
      <% } %>
    </div>

    <!-- Search bar — mobile only, wraps to its own row. Hidden on md+ where hero/page search is visible -->
    <% if(isSearch) {%>
      <div class="search-bar d-flex d-md-none align-items-center w-100 order-last mt-1" data-bs-toggle="modal"
        data-bs-target="#filterModal">
        <div class="position-relative w-100">
          <input type="text" class="form-control border-0 shadow-none search-input" placeholder="Search destination">
          <i class="bi bi-search position-absolute top-50 end-0 translate-middle-y search-icon me-3"
            aria-hidden="true"></i>
        </div>
      </div>
    <%}%>

    <!-- Desktop collapsed nav -->
    <div class="collapse navbar-collapse" id="topNavbar">
      <ul class="navbar-nav gap-lg-3 ms-auto mt-2 mt-lg-0 align-items-lg-center">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/triplist">Tour packages</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/dreamtrip">Customize trips</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="/triplist">Global trips</a>
        </li>
        <li class="nav-item d-flex align-items-center">
          <a class="nav-link rounded-pill fw-semibold contactLink" href="tel:+918511117891">
            <i class="bi bi-telephone"></i> +918511117891
          </a>
        </li>
        <% if(profile?.email || accessToken) { %>
          <li class="nav-item">
            <span class="dropdown">
              <a class="nav-link userDropdown dropdown-toggle" href="#" role="button"
                data-bs-toggle="dropdown" aria-expanded="false">
                <i class="bi bi-person-circle nav-icon"></i>
                <span class="visually-hidden">My Profile</span>
              </a>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <ul class="dropdown-menu dropdown-menu-lg-end navbaruserdropdown">
                <li><a class="dropdown-item" href="/profile">My Profile</a></li>
                <% if(profile?.email===process.env.GMAIL_ADMIN || profile?.email===process.env.GMAIL_ADMIN_TWO) { %>
                  <li><a class="dropdown-item" href="/admin/bookings" target="_blank">Bookings</a></li>
                  <li>
                    <a class="dropdown-item position-relative" href="/admin/booking-leads" target="_blank">
                      Booking Leads
                      <span id="pendingLeadsBadge" class="position-absolute top-50 end-0 translate-middle-y badge rounded-pill bg-danger" style="display: none;">
                        <span id="pendingLeadsCount">0</span>
                      </span>
                    </a>
                  </li>
                  <li><a class="dropdown-item" href="/admin/bookings/create" target="_blank">Create Booking</a></li>
                  <li><a class="dropdown-item" href="/admin/users" target="_blank">User</a></li>
                  <li><a class="dropdown-item" href="/admin/addtours" target="_blank">Add Tours</a></li>
                  <li><a class="dropdown-item" href="/cities" target="_blank">Master setting</a></li>
                  <li><a class="dropdown-item" href="/admin/category-trip-order" target="_blank">Homepage Categories</a></li>
                <% } %>
                <li>
                  <form id="my_form" action="/logout" method="post">
                    <a class="dropdown-item" href="javascript:{}" onclick="signout()">Sign Out</a>
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                  </form>
                </li>
              </ul>
            </span>
          </li>
        <% } else { %>
          <li class="nav-item">
            <a class="nav-link loginBtn" role="button" tabindex="-1" onclick="openLogin()">Login</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
<!-- Signin Modal -->
<div class="modal fade" id="signinModal" tabindex="-1" role="dialog" aria-labelledby="signinModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content position-relative">
      <button type="button" class="auth-close" data-bs-dismiss="modal" aria-label="Close">&#x2715;</button>
      <div class="modal-body">
        <form id="getotp">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <img src="/img/logo_2x.png" alt="Deja-vu Tours" class="auth-logo" />
          <p class="auth-welcome">Welcome to</p>
          <p class="auth-brand" id="signinModalLabel">Deja vu Tours</p>
          <div class="auth-divider"><span>Log in or Sign up</span></div>
          <div class="auth-phone-group">
            <span class="auth-phone-prefix">+91</span>
            <input type="tel" class="auth-phone-input" id="mobileNumber" name="phone"
              placeholder="Enter Mobile Number" pattern="^\d{10}$" required />
          </div>
          <button type="submit" class="auth-continue-btn" id="sendCodeBtn" disabled>Continue</button>
          <div class="auth-or-divider"><span>or</span></div>
          <a href="#" id="googleLoginLink" class="auth-google-btn">
            <img src="data:image/svg+xml;base64,"
              width="18" height="18" alt="Google" />
            Continue with Google
          </a>
          <p class="auth-legal">
            By continuing, you agree to our<br />
            <a href="/terms" target="_blank">Terms &amp; Conditions</a>
            &nbsp;&nbsp;
            <a href="/privacy" target="_blank">Privacy Policy</a>
          </p>
        </form>
      </div>
    </div>
  </div>
</div>
<!-- Verify OTP Modal -->
<div class="modal fade" id="verifyModal" tabindex="-1" aria-labelledby="verifyModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content position-relative">
      <button type="button" class="auth-close" data-bs-dismiss="modal" aria-label="Close">&#x2715;</button>
      <div class="modal-body">
        <form id="verifyotp">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <img src="/img/logo_2x.png" alt="Deja-vu Tours" class="auth-logo-sm" />
          <h6 class="auth-otp-title" id="verifyModalLabel">Verify your number</h6>
          <p class="auth-otp-sub">
            Code sent to <strong id="phoneNumberDisplay">+91</strong>
            <span class="edit-icon" id="editPhone">✎</span>
          </p>
          <div class="otp-container">
            <input type="text" class="otp-input" maxlength="1" id="otp1" required />
            <input type="text" class="otp-input" maxlength="1" id="otp2" required />
            <input type="text" class="otp-input" maxlength="1" id="otp3" required />
            <input type="text" class="otp-input" maxlength="1" id="otp4" required />
            <input type="hidden" id="mobileNumber2" name="phone2" />
            <input type="hidden" id="otp" name="otp" />
          </div>
          <button type="submit" class="auth-verify-btn" id="verifyBtn" disabled>Verify code</button>
          <p class="mb-0" style="font-size:0.85rem;color:#6b7280;">
            Didn't receive code? <a href="#" class="resend-link">Resend</a>
          </p>
        </form>
      </div>
    </div>
  </div>
</div>
<script>
  // Navigation bar active link
  document.addEventListener("DOMContentLoaded", () => {
    const currentPath = window.location.pathname;
    const parentPaths = {
      '/triplist': ['/triplist', '/tripdetail', '/booktour'],
      '/dreamtrip': ['/dreamtrip'],
      '/': ['/'],
    };
    document.querySelectorAll("#topNavbar .nav-link[href]").forEach(link => {
      const href = link.getAttribute("href");
      if (!href || href.startsWith("tel:") || href === "#") return;
      const matches = parentPaths[href] || [href];
      const isActive = matches.some(p => p === "/" ? currentPath === "/" : currentPath.startsWith(p));
      if (isActive) link.classList.add("active");
    });
  });

  function openLogin() {
    bootstrap.Modal.getOrCreateInstance(document.getElementById('signinModal')).show();
  }

  // Autocomplete function for search input
  function autocomplete(inp, arr) {
    var currentFocus;
    inp.addEventListener("input", function (e) {
      var a, b, i, val = this.value;
      closeAllLists();
      if (!val) {
        return false;
      }
      currentFocus = -1;
      a = document.createElement("DIV");
      a.setAttribute("id", this.id + "autocomplete-list");
      a.setAttribute("class", "autocomplete-items");
      this.parentNode.appendChild(a);
      for (i = 0; i < arr.length; i++) {
        if (arr[i].substr(0, val.length).toUpperCase() == val.toUpperCase()) {
          b = document.createElement("DIV");
          b.innerHTML = "<strong>" + arr[i].substr(0, val.length) + "</strong>";
          b.innerHTML += arr[i].substr(val.length);
          b.innerHTML += "<input type='hidden' value='" + arr[i] + "'>";
          b.addEventListener("click", function (e) {
            inp.value = this.getElementsByTagName("input")[0].value;
            closeAllLists();
          });
          a.appendChild(b);
        }
      }
    });
    inp.addEventListener("keydown", function (e) {
      var x = document.getElementById(this.id + "autocomplete-list");
      if (x) x = x.getElementsByTagName("div");
      if (e.keyCode == 40) {
        currentFocus++;
        addActive(x);
      } else if (e.keyCode == 38) {
        currentFocus--;
        addActive(x);
      } else if (e.keyCode == 13) {
        e.preventDefault();
        if (currentFocus > -1) {
          if (x) x[currentFocus].click();
        }
      }
    });
    function addActive(x) {
      if (!x) return false;
      removeActive(x);
      if (currentFocus >= x.length) currentFocus = 0;
      if (currentFocus < 0) currentFocus = (x.length - 1);
      x[currentFocus].classList.add("autocomplete-active");
    }
    function removeActive(x) {
      for (var i = 0; i < x.length; i++) {
        x[i].classList.remove("autocomplete-active");
      }
    }
    function closeAllLists(elmnt) {
      var x = document.getElementsByClassName("autocomplete-items");
      for (var i = 0; i < x.length; i++) {
        if (elmnt != x[i] && elmnt != inp) {
          x[i].parentNode.removeChild(x[i]);
        }
      }
    }
    document.addEventListener("click", function (e) {
      closeAllLists(e.target);
    });
  }

  async function signout(accessToken) {
    try {
      sessionStorage.clear();
      localStorage.clear();
      document.cookie.split(";").forEach(cookie => {
        const name = cookie.split("=")[0].trim();
        document.cookie = `${name}=; Path=/; Expires=Thu, 01 Jan 1970 00:00:00 GMT; SameSite=Lax`;
      });
      const form = document.getElementById("my_form");
      const response = await fetch("/logout", {
        method: "POST",
        body: new FormData(form),
        credentials: "include"
      });
      if (response.ok) {
        window.location.href = "/";
      } else {
        console.error("Logout failed:", response.statusText);
        alert("Logout failed. Please try again.");
      }
    } catch (error) {
      console.error("Logout error:", error);
      alert("An error occurred during logout. Please try again.");
    }
  }

  document.getElementById("mobileNumber").addEventListener("input", function () {
    const sendCodeBtn = document.getElementById("sendCodeBtn");
    const isValid = /^\d{10}$/.test(this.value);
    sendCodeBtn.disabled = !isValid;
    sendCodeBtn.classList.toggle("btn-disabled", !isValid);
  });

  document.addEventListener("DOMContentLoaded", function () {
    // Handle Google login link with current URL
    const googleLoginLink = document.getElementById("googleLoginLink");
    if (googleLoginLink) {
      googleLoginLink.addEventListener("click", function (e) {
        e.preventDefault();
        const currentUrl = window.location.pathname + window.location.search;
        const googleLoginUrl = `/login/federated/google?returnTo=${encodeURIComponent(currentUrl)}`;
        window.location.href = googleLoginUrl;
      });
    }

    const getOtpForm = document.getElementById("getotp");
    const verifyForm = document.getElementById("verifyotp");
    const verifyBtn = document.getElementById("verifyBtn");
    const resendLink = document.querySelector(".resend-link");
    const otpInputs = document.querySelectorAll(".otp-input");
    const sendCodeBtn = document.getElementById("sendCodeBtn");
    const timerDisplay = document.createElement("span");
    resendLink.parentElement.appendChild(timerDisplay);
    let resendTimer;
    const editPhoneIcon = document.getElementById("editPhone");

    editPhoneIcon.addEventListener("click", function () {
      const phoneNumber = document.getElementById("phoneNumberDisplay").textContent.replace('+91 ', '');
      document.getElementById("mobileNumber").value = phoneNumber;
      $("#verifyModal").modal("hide");
      $("#signinModal").modal("show");
    });

    function requestOTP(mobileNumber) {
      if (!mobileNumber || !/^(\+?\d{1,3}[-\s]?)?\d{10}$/.test(mobileNumber)) {
        return alert("Enter a valid mobile number!");
      }
      var formData = new FormData(getOtpForm || verifyForm);
      formData.delete('phone');
      formData.append('phone', mobileNumber);
      fetch("/getotp", {
        method: "POST",
        body: formData
      })
        .then((response) => response.json())
        .then((data) => {
          if (data) {
            document.getElementById("mobileNumber2").value = mobileNumber;
            document.getElementById("phoneNumberDisplay").textContent = "+91 " + mobileNumber;
            $("#signinModal").modal("hide");
            $("#verifyModal").modal("show");
          } else {
            alert("Failed to send OTP. Please try again.");
          }
        })
        .catch((error) => console.log("Error:", error));
    }

    getOtpForm.addEventListener("submit", function (event) {
      event.preventDefault();
      const mobileNumber = document.getElementById("mobileNumber").value;
      requestOTP(mobileNumber);
    });

    resendLink.addEventListener("click", function (e) {
      e.preventDefault();
      startResendTimer();
      const phoneNumber = document.getElementById("mobileNumber2").value;
      requestOTP(phoneNumber);
    });

    otpInputs.forEach((input, index) => {
      input.addEventListener("input", function () {
        this.value = this.value.replace(/\D/g, "");
        if (this.value && index < otpInputs.length - 1) {
          otpInputs[index + 1].focus();
        }
        validateOTP();
      });
      input.addEventListener("keydown", function (e) {
        if (e.key === "Backspace" && !this.value && index > 0) {
          otpInputs[index - 1].focus();
        }
      });
    });

    function validateOTP() {
      const allFilled = [...otpInputs].every((input) => /^[0-9]$/.test(input.value));
      verifyBtn.disabled = !allFilled;
      verifyBtn.classList.toggle("btn-disabled", !allFilled);
    }

    verifyForm.addEventListener("submit", function (event) {
      event.preventDefault();
      verifyBtn.innerHTML = "Verifying...";
      verifyBtn.disabled = true;
      const fullOtp = [...otpInputs].map((input) => input.value).join("");
      document.getElementById("otp").value = fullOtp;
      fetch("/verifyotp", {
        method: "POST",
        body: new FormData(verifyForm)
      })
        .then((response) => response.json())
        .then((data) => {
          if (data) {
            sessionStorage.setItem("accessToken", data.accessToken);
            document.cookie = `accessToken=${data.accessToken}; Path=/; SameSite=Lax`;
            window.location.reload();
          } else {
            alert("Verification failed. Please try again.");
          }
        })
        .catch(error => console.error("Error:", error))
        .finally(() => {
          verifyBtn.innerHTML = "Verify Code";
          verifyBtn.disabled = false;
        });
    });

    function startResendTimer() {
      resendLink.style.pointerEvents = "none";
      let timeLeft = 10;
      resendTimer = setInterval(() => {
        if (timeLeft <= 0) {
          clearInterval(resendTimer);
          timerDisplay.innerHTML = "";
          resendLink.style.pointerEvents = "auto";
        } else {
          timerDisplay.innerHTML = ` (Wait ${timeLeft--}s)`;
        }
      }, 1000);
    }

    // Fetch and update pending leads badge
    <% if(profile?.email === process.env.GMAIL_ADMIN || profile?.email === process.env.GMAIL_ADMIN_TWO) { %>
    async function updatePendingLeadsBadge() {
      try {
        const response = await fetch('/admin/booking-leads/stats', {
          credentials: 'include'
        });
        if (response.ok) {
          const data = await response.json();
          if (data.success && data.stats.pending > 0) {
            const badge = document.getElementById('pendingLeadsBadge');
            const count = document.getElementById('pendingLeadsCount');
            if (badge && count) {
              count.textContent = data.stats.pending;
              badge.style.display = 'inline-block';
            }
          }
        }
      } catch (error) {
        console.error('Error fetching pending leads count:', error);
      }
    }
    updatePendingLeadsBadge();
    <% } %>
  });
</script>